const { expect } = require('chai');
const { companyAdministration } = require('./companyAdministration');

describe ('My tests', () => {
    it('test 1', () => {
        expect(1).equal(1);
    })
})